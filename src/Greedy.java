public class Greedy extends Solver {
    public Greedy(int target, int[] pizzas) {
        super(target, pizzas);
    }

    @Override
    public int[] getSolution() {
        // greedy solution
        return new int[0];
    }
}
